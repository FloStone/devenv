# Postgres
DB_CONNECTION=pgsql
DB_HOST=postgres-18
DB_PORT=5432
DB_DATABASE=
DB_USERNAME=root
DB_PASSWORD=

# MongoDB
MONGODB_PORT=27017
MONGODB_URI=mongodb://mongodb-8:27017/

# Redis
REDIS_CLIENT=phpredis
REDIS_HOST=valkey
REDIS_PASSWORD=null
REDIS_PORT=6379

# Mail
MAIL_MAILER=smtp
MAIL_HOST=mailpit
MAIL_PORT=2525

# MinIO
AWS_ACCESS_KEY_ID=root
AWS_SECRET_ACCESS_KEY=minioroot
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=hearlive
AWS_USE_PATH_STYLE_ENDPOINT=true
AWS_URL=http://minio.local/hearlive
AWS_ENDPOINT=http://minio:9000

# Meilisearch
MEILISEARCH_HOST=http://meilisearch:7700
MEILISEARCH_KEY=

# Keycloak
KEYCLOAK_CLIENT_ID=
KEYCLOAK_CLIENT_SECRET=
KEYCLOAK_BASE_URL="http://keycloak.local"
KEYCLOAK_REALM=
KEYCLOAK_REDIRECT_URI=
